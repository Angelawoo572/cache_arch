# 602 matched-input baseline.
#
# These values intentionally reproduce the compiled defaults used by the
# tracked prefetcher-baseline sweep.  ChampSim's generic config/stride.ini uses
# 256 trackers, so that file is not the configuration behind the retained
# 602 stride result.
l2c_prefetcher_types = stride
stride_num_trackers = 64
stride_pref_degree = 2
