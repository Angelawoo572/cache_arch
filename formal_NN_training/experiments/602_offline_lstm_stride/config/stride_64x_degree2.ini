# ChampSim fd26fc51 parses these as top-level INI keys (no [section] headers).
# Used only as a provenance record; run_server.sh passes the same values
# explicitly and rejects a run if the stride prefetcher is not active.
l2c_prefetcher_types = stride
stride_num_trackers = 64
stride_pref_degree = 2
