\documentclass[10pt]{article}
\usepackage[margin=0.72in]{geometry}
\usepackage{booktabs,tabularx,xcolor,amsmath}
\setlength{\parindent}{0pt}
\definecolor{warn}{RGB}{175,25,25}
\begin{document}
\begin{center}
{\Large\bfseries 602 Direct-Action LSTM vs. Offline Stride}\\[4pt]
{\color{warn}\bfseries RERUN REQUIRED --- legacy gate results are superseded}
\end{center}

The previous PDF measured an LSTM \emph{gate} over Stride-generated
candidates.  That architecture did not satisfy the intended experiment:
matching the normal prefetcher's external input while allowing the neural
prefetcher to generate its own actions.  Its numerical tables and plots must
not be presented as direct-action results.

\begin{center}\small
\begin{tabularx}{\textwidth}{@{}lX@{}}
\toprule
Shared external input & Current PC and cache-line address, with causal prior PC/address history.\\
Neural policy & Standalone stateful LSTM with a 64-offset direct-action head; at most two same-page L2 actions per callback.\\
Excluded & Stride candidates, tracker table, last stride, confidence, hit/miss, cycle, queues, and future evaluation rows.\\
Supervision & Future-demand utility labels formed only inside the training stream.\\
Comparator use & Captured/mirrored Stride actions are used only for normal replay and the training-split request budget.\\
Required run & \texttt{602\_offline\_lstm\_stride\_direct\_v3\_seed7}.\\
\bottomrule
\end{tabularx}
\end{center}

After the direct-v3 replay passes, regenerate the report with IPC, L2 miss
rate, selected accuracy, coverage, late-prefetch rate, and the balanced parity
index.  No old gate number is carried forward.
\end{document}
