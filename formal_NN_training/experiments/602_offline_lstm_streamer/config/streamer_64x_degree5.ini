# Exact streamer configuration used by the 602 matched-input experiment.
# run_server.sh passes these as explicit top-level flags because this ChampSim
# parser does not load sectioned INI files automatically.
streamer_num_trackers = 64
streamer_pref_degree = 5

