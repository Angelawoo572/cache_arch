\documentclass[10pt]{article}
\usepackage[margin=0.72in]{geometry}
\usepackage{booktabs,tabularx,xcolor,amsmath}
\setlength{\parindent}{0pt}
\definecolor{warn}{RGB}{175,25,25}
\begin{document}
\begin{center}
{\Large\bfseries 602 Direct-Action LSTM vs. Offline Streamer}\\[4pt]
{\color{warn}\bfseries RERUN REQUIRED --- legacy gate results are superseded}
\end{center}

The previous PDF measured an LSTM \emph{gate} over Streamer-generated
candidates.  It therefore coupled the neural action space to the normal
prefetcher's private algorithm.  Its numerical tables and plots are retained
in Git history only and must not be presented as standalone-NN results.

\begin{center}\small
\begin{tabularx}{\textwidth}{@{}lX@{}}
\toprule
Shared external input & Current cache-line address and causal prior address history; PC remains replay identity only.\\
Neural policy & Standalone stateful LSTM with a 64-offset direct-action head; at most five same-page L2 actions per callback.\\
Excluded & Streamer candidates, page trackers, direction state, hit/miss, cycle, queues, and future evaluation rows.\\
Supervision & Future-demand utility labels formed only inside the training stream.\\
Comparator use & Mirrored Streamer actions are used only for normal replay and the training-split request budget.\\
Required run & \texttt{602\_offline\_lstm\_streamer\_direct\_v3\_seed7}.\\
\bottomrule
\end{tabularx}
\end{center}

After the direct-v3 replay passes, regenerate the report with IPC, L2 miss
rate, selected accuracy, coverage, late-prefetch rate, and balanced parity.
No old gate number is carried forward.
\end{document}
