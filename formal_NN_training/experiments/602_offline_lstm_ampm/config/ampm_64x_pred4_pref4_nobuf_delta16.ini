# Pinned first 602 AMPM matched-input baseline configuration.
# These are the compiled knobs actually used by the retained AMPM matrix,
# made explicit here so the new experiment cannot silently depend on defaults.
ampm_pb_size = 64
ampm_pred_degree = 4
ampm_pref_degree = 4
ampm_enable_pref_buffer = false
ampm_pref_buffer_size = 256
ampm_max_delta = 16
