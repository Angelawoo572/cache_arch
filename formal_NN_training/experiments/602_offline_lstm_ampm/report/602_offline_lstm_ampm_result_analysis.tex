\documentclass[10pt]{article}
\usepackage[margin=0.72in]{geometry}
\usepackage{booktabs,tabularx,xcolor,amsmath}
\setlength{\parindent}{0pt}
\definecolor{warn}{RGB}{175,25,25}
\begin{document}
\begin{center}
{\Large\bfseries 602 Direct-Action LSTM vs. Offline AMPM}\\[4pt]
{\color{warn}\bfseries RERUN REQUIRED --- legacy mirror/gate results are superseded}
\end{center}

The previous PDF allowed the neural path to consume AMPM-equivalent
bitmap/LRU state and AMPM-generated candidates.  Those are private normal
prefetcher internals, not shared external inputs.  Its numerical tables and
plots must not be presented as standalone-NN results.

\begin{center}\small
\begin{tabularx}{\textwidth}{@{}lX@{}}
\toprule
Shared external input & Current cache-line address and causal prior address history; PC remains replay identity only.\\
Neural policy & Standalone stateful LSTM; learned count argmax plus learned ranking over all 64 same-page L2 targets.\\
Excluded & AMPM candidates, 64-page bitmap/LRU state, hit/miss, cycle, queues, and future evaluation rows.\\
Supervision & Mirrored AMPM request count and target set; labels only, never inference inputs.\\
No heuristic decision & No probability threshold, request budget, degree cap, future window, semantic hand features, manual loss weights, or regularizer.\\
Guard & The guard initializes AMPM and LSTM state independently; neither state is shared.\\
Required run & \texttt{602\_offline\_lstm\_ampm\_threshold\_free\_v5\_seed7}.\\
\bottomrule
\end{tabularx}
\end{center}

After the threshold-free-v5 replay passes, regenerate the report with IPC, L2 miss
rate, selected accuracy, coverage, late-prefetch rate, and balanced parity.
No old mirror/gate number is carried forward.
\end{document}
